import { customElement, LitElement, html } from "lit-element";

@customElement('elliott-nav-section')
class ElliottNavSection extends LitElement {
  render() {
    return html`
      <div>NAVIGATIONZZZ</div>
    `;
  }
}