{
  "database": {
    "rules": "database.rules.json"
  },
  "hosting": {
    "public": ["build/bundled", "img"],
    "rewrites": [
      {
        "source": "/main",
        "destination": "/index.html"
      },
      {
        "source": "/project/*",
        "destination": "/index.html"
      }
    ]
  }
}
